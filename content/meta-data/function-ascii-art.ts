import { MetaTags } from './types';
import { Routes } from '../routes';

const metaTags: MetaTags = {
  title: 'ASCII Art Generator',
  description: 'Convert images to ASCII art using OpenCV.js with customizable character sets and density mapping. Create text-based artwork from any image.',
  keywords: ['ASCII Art', 'Text Art', 'Image to Text', 'OpenCV', 'Character Mapping', 'Text Generator', 'Art Converter'],
  url: Routes.AsciiArtTool,
};

export { metaTags };
