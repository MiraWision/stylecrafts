import { MetaTags } from './types';
import { Routes } from '../routes';

const metaTags: MetaTags = {
  title: 'EXIF Metadata Viewer & Cleaner',
  description: 'View and remove EXIF metadata from your images. Check camera settings, GPS location, and other metadata, then download clean copies without personal data.',
  keywords: ['EXIF Viewer', 'Metadata Cleaner', 'EXIF Remover', 'Image Metadata', 'GPS Data', 'Camera Settings', 'Privacy Tool', 'EXIF Cleaner'],
  url: Routes.ExifTool,
};

export { metaTags };
