import { MetaTags } from './types';
import { Routes } from '../routes';

const metaTags: MetaTags = {
  title: 'Background Removal',
  description: 'Remove backgrounds from images using AI-powered OpenCV algorithms. Clean, professional background removal for photos and graphics.',
  keywords: ['Background Removal', 'Image Editing', 'Photo Editing', 'OpenCV', 'AI', 'Transparent Background'],
  url: Routes.BackgroundRemovalTool,
};

export { metaTags };
